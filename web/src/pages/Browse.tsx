import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;

const Browse = () => {
  return (
    <Title level={1}>Hello from browse</Title>
  );
};

export default Browse;
